includes:
    - ./vendor/nunomaduro/larastan/extension.neon
parameters:
    paths:
        - app
        - public
        - routes
        - tests
        - config
        - database
    level: 5
    ignoreErrors:
        # Bu modellar ataylab kompozit birlamchi kalit ishlatadi
        # (App\Core\Helpers\Traits\HasCompositePrimaryKey). Eloquent
        # bazaviy modelida $primaryKey `string` deb belgilangan, shuning
        # uchun kovariantlik xatosi muqarrar. Bu bostirish emas - xato
        # aynan shu xabar va aynan shu kataloq bilan cheklangan.
        -
            message: '#PHPDoc type array<int, string> of property .* is not covariant with PHPDoc type string of overridden property .*primaryKey#'
            path: app/Models/Links

        # `Illuminate\Database\Seeder::$command` tur bildirilmagan
        # xususiyat va konsoldan tashqarida `null` bo'ladi, lekin
        # framework uni docblokda non-nullable deb e'lon qilgan.
        # Laravel'ning O'ZI ham Seeder.php:52 da `isset($this->command)`
        # bilan tekshiradi, ya'ni annotatsiya noto'g'ri. Istisno bitta
        # satr bilan cheklangan.
        # (`path` ishlatilmaydi: xato trait faylida, lekin uni ishlatuvchi
        # har bir seeder klassi kontekstida qayta xabar qilinadi.)
        - '#Property .*Seeder::\$command .* on left side of \?\? is not nullable#'
    excludePaths:
        # Auth fayllari bo'yicha istisnolar OLIB TASHLANDI: aynan o'sha
        # fayllar xavfsizlik uchun eng muhimi va tahlildan chetda
        # qolmasligi kerak edi.
        - routes/console.php
